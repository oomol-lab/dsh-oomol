save-exact=true
# DeepSeek Harness rc packages publish their runtime composition as peers.
# Install them in the development fixture so the isolated Web smoke test uses
# the same complete graph as a real Harness installation.
auto-install-peers=true
